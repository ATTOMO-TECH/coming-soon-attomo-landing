[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA fallback — function routes (e.g. /api/holded-contact) are matched first.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
